/*global describe, it */
(function () {
  'use strict';

  describe('Parallax', function() {
    describe('Position value according to scroll value', function() {
      it('should be kept within bounds of displaying area and cover area', function() {

        expect('foo').to.be.a('string', 'foo is a string');

      });
    });
  });
})();
